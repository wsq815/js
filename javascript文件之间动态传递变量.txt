javascript文件之间的动态变量传递问题有两种情况：

第一种：页面不发生调转的情况下，可以利用window使变量成为全局变量，为了不污染全局对象建议在window里面建一个对象，再通过该对象传递变量。

第二种：页面发生了跳转，作用域刷新了，也就没有全局变量的动态变化了，这时我们采用另外一种方法，在跳转地址上传参 xxx.com?vid=xxx,再利用正则取参的方式把vid获取下来，例子：腾讯视频首页点击某个视频，到内容页具体播放某个视频。